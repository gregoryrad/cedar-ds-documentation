---
import BaseLayout from '../../layouts/BaseLayout.astro';
import PageHeader from '../../components/PageHeader.astro';
import PageContent from '../../components/PageContent.astro';
import { ProgressBar } from '../../cedar/components/ProgressBar/ProgressBar';
---

<BaseLayout title="Progress Bar">
  <PageHeader
    eyebrow="Components"
    title="Progress Bar"
    description="Determinate progress indicator for async operations. Three sizes with pill-end corners, token-driven colors."
  />

  <PageContent>
    <section>
      <h2 class="section-title">Sizes</h2>
      <p class="section-desc">Track is bg/muted; fill is action/primary (teal).</p>
      <div class="demo-block progress-demo">
        <div class="progress-row">
          <code class="label">sm · 4px</code>
          <div class="progress-wrap">
            <ProgressBar value={60} size="sm" client:load />
          </div>
          <code class="label">60%</code>
        </div>
        <div class="progress-row">
          <code class="label">md · 8px</code>
          <div class="progress-wrap">
            <ProgressBar value={40} size="md" client:load />
          </div>
          <code class="label">40%</code>
        </div>
        <div class="progress-row">
          <code class="label">lg · 12px</code>
          <div class="progress-wrap">
            <ProgressBar value={75} size="lg" client:load />
          </div>
          <code class="label">75%</code>
        </div>
      </div>
    </section>

    <section>
      <h2 class="section-title">Tokens used</h2>
      <div class="token-table">
        {[
          { token: 'action/primary', usage: 'Progress bar fill' },
          { token: 'bg/muted',       usage: 'Progress bar track background' },
        ].map(row => (
          <div class="token-row">
            <code class="token-name">{row.token}</code>
            <span class="token-usage">{row.usage}</span>
          </div>
        ))}
      </div>
    </section>
  </PageContent>
</BaseLayout>

<style>
  .section-title {
    font-size: var(--heading-lg);
    font-weight: 700;
    color: var(--text-base);
    margin-bottom: var(--space-4);
  }

  .section-desc {
    font-size: var(--body-sm);
    color: var(--text-subtle);
    max-width: 560px;
    line-height: 1.6;
    margin-bottom: var(--space-4);
  }

  .demo-block {
    background: var(--bg-base);
    border: var(--border-width-base) solid var(--border-subtle);
    border-radius: var(--radius-xl);
    padding: var(--space-6);
  }

  .progress-demo {
    display: flex;
    flex-direction: column;
    gap: var(--space-5);
    max-width: 480px;
  }

  .progress-row {
    display: flex;
    align-items: center;
    gap: var(--space-4);
  }

  .progress-wrap {
    flex: 1;
  }

  .label {
    font-family: var(--font-mono);
    font-size: var(--size-xs);
    color: var(--text-subtle);
  }

  .token-table {
    border: var(--border-width-base) solid var(--border-subtle);
    border-radius: var(--radius-lg);
    overflow: hidden;
    background: var(--bg-base);
  }

  .token-row {
    display: grid;
    grid-template-columns: 2fr 3fr;
    gap: var(--space-4);
    padding: var(--space-3) var(--space-4);
    border-bottom: var(--border-width-base) solid var(--border-subtle);
    align-items: center;
  }

  .token-row:last-child { border-bottom: none; }
  .token-row:hover { background: var(--bg-faint); }

  .token-name {
    font-family: var(--font-mono);
    font-size: var(--size-xs);
    color: var(--text-base);
  }

  .token-usage {
    font-size: var(--body-sm);
    color: var(--text-subtle);
  }
</style>
